# Server Configuration
PORT=8765
HOST=127.0.0.1
NODE_ENV=development

# AI Analyst Configuration (Backend Only)
# Optional: Set your Gemini API key to enable live LLM legal insights
# If omitted, LandGuard automatically runs in deterministic local legal explanation mode
GEMINI_API_KEY=

# Rate Limiting (Requests per minute per IP)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=60
